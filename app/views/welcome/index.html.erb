<h2>WebMonitor</h2>
<table class="ranking">
  <thead>
    <tr>
      <th>
        Name
      </th>
      <th>
        Address
      </th>
    </tr>
  </thead>
  <tbody>
    <% @sites.each do |site| %>
    <tr>
      <td>
        <%= site.name %>
      </td>
      <td>
       <a href="<%= site.url %>"><%= site.url %></a>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
